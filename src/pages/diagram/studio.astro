---
import ToolPageLayout from '../../components/ToolPageLayout.astro';
import DiagramStudioWorkspace from '../../components/workspaces/DiagramStudioWorkspace';
import { TOOLS } from '../../lib/toolsData';

const tool = TOOLS.find(t => t.id === 'diagram-studio')!;
---

<ToolPageLayout tool={tool}>
  <DiagramStudioWorkspace client:load />
</ToolPageLayout>
