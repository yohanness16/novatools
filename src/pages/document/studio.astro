---
import ToolPageLayout from '../../components/ToolPageLayout.astro';
import DocStudioWorkspace from '../../components/workspaces/DocStudioWorkspace';
import { TOOLS } from '../../lib/toolsData';

const tool = TOOLS.find(t => t.id === 'doc-studio')!;
---

<ToolPageLayout tool={tool}>
  <DocStudioWorkspace client:load />
</ToolPageLayout>
