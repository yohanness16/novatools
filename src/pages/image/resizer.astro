---
import ToolPageLayout from '../../components/ToolPageLayout.astro';
import { ImageResizerWorkspace } from '../../components/workspaces/ImageResizerWorkspace';
import { TOOLS } from '../../lib/toolsData';

const tool = TOOLS.find(t => t.id === 'image-resizer')!;
---

<ToolPageLayout tool={tool}>
  <ImageResizerWorkspace client:load />
</ToolPageLayout>
